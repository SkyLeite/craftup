setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: ./backend/Dockerfile
release:
  command:
    - mix ecto.migrate && exit
  image: web
run:
  web:
    command:
      - mix phx.server
    image: web
